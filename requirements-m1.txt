# Additional dependencies for M1 real-feed connectors (CHIRPS / MODIS).
# Install after requirements.txt, or via: pip install -e ".[rs]"
rasterio>=1.3
shapely>=2.0
pyproj>=3.6
requests>=2.32
pyhdf>=0.11.6  # MOD13A1 is HDF4; rasterio wheels lack the GDAL HDF4 driver
